"user server"

export const signIn = async () => {
    try{

    } catch(err){
        console.log("Error:",err)
    }
}

export const signUp = async (userData : SignUpParams) => {
    try{
        //create user Account 
    } catch(err){
        console.log("Error:",err)
    }
}